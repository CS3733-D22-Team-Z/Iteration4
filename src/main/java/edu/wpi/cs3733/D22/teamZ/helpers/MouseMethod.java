package edu.wpi.cs3733.D22.teamZ.helpers;

import javafx.scene.input.MouseEvent;

/** Represents a method that can be called with a MouseEvent as its parameter. */
public interface MouseMethod {
  void call(MouseEvent event);
}
